---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: traefik
spec:
  interval: 10m
  install:
    createNamespace: true
  chart:
    spec:
      chart: traefik
      version: '41.6.0'
      sourceRef:
        kind: HelmRepository
        name: traefik-repository
        namespace: traefik
  values:
    service:
      annotations:
        metallb.io/ip-address-pool: first-pool
      spec:
        externalTrafficPolicy: Local
    ingressRoute:
      dashboard:
        enabled: false
    providers:
      kubernetesCRD:
        allowCrossNamespace: true
        allowExternalNameServices: true
    ports:
      web:
        forwardedHeaders:
          trustedIPs: &trustedForwardedIPs
            - 10.10.93.0/24
        http:
          aliasHeadersStrategy: delete
      websecure:
        forwardedHeaders:
          trustedIPs: *trustedForwardedIPs
        http:
          aliasHeadersStrategy: delete
