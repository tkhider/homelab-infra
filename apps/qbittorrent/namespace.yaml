apiVersion: v1
kind: Namespace
metadata:
  name: downloads
  labels:
    # gluetun needs NET_ADMIN to bring up the VPN tunnel and its firewall,
    # which the Talos default (baseline) rejects. Kept to this namespace only.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/warn: baseline
